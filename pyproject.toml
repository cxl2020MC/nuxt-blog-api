[project]
name = "blog-api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiohttp",
    "fastapi",
    "motor",
    "passlib[bcrypt]",
    "pyjwt[crypto]",
    "python-dotenv",
    "python-multipart",
    "uvicorn[standard]",
]
